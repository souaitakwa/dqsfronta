export interface question {
    id: number;
    title: string;
    description: string;
    userid: number;
    postdate : Date;
  }
  export interface question {
    id: number;
    title: string;
    description: string;
  }